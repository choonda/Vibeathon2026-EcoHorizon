# Module 5: Post-Trip Dashboard & Carbon Ledger

## Setup & Data Layer
- [x] Create the `trips` table in Supabase (`id`, `user_id`, `distance_km`, `eco_score`, `fuel_cost_rm`, `carbon_saved_kg`).
- [x] Create the `TripModel` Dart data class.
- [x] Create the abstract `TripRepository` interface.
- [x] Implement `SupabaseTripRepo` to push the final JSON payload to the cloud.

## State Management (Riverpod)
- [x] Create the `LedgerController` (`StateNotifier`).
- [x] Implement a translator function to map saved carbon float values (kg CO2) into concrete UI strings (e.g., "1 Tree Planted in Johor Bahru").
- [x] Implement logic to automatically update the user's `petrol_points_balance` upon a successful trip upload.

## Presentation Layer (UI)
- [x] Build the `TripSummaryScreen` to display the final Eco Score, total distance, and financial breakdown (RM lost/saved).
- [x] Build the `CarbonLedgerDashboard` widget (Main App Home).
- [x] Use `fl_chart` to render a historical trend graph of the user's past trips.
- [x] Bind the dashboard text blocks to display the concrete environmental impact strings generated by the `LedgerController`.